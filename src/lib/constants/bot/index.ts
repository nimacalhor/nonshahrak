import * as buttonLabels from "./button-labels";
import * as general from "./general";
import * as sessions from "./session";

const exportObj = {
  ...buttonLabels,
  ...general,
  ...sessions,
};

export default exportObj;
